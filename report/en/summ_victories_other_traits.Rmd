### `r other_traits_list[[i]]$code`

```{r}
vic_i <- vic_other_traits[[i]]
```

\

```{r,dpi=dpi, fig.height=contest_h, fig.width=9, fig.cap=paste0("2.2.",i, ". Head to head performance for '",tolower(other_traits_list[[i]]$fullname),"'.")}
plot(vic_i) + 
  labs(y = "", x = "") + 
  theme_minimal() +  
  theme(axis.text.x = element_text(size = 10, color = "#000000"),
        axis.text.y = element_text(size = 10, color = "#000000"),
        strip.text.x = element_text(size = 11, color = "#000000", face = "bold"),
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank(),
        legend.position = "none")

```