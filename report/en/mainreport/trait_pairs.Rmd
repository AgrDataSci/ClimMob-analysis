**`r other_traits_list[[i]]$fullname`**

```{r}
if (ncomp == 3) {
  RR <- rank_tricot(data = cmdata[other_traits_list[[i]]$keep, ],
                    items = itemnames,
                    input = other_traits_list[[i]]$input)
}

if (ncomp > 3) {
  RR <- rank_numeric(data = cmdata[other_traits_list[[i]]$keep, ],
                     items = itemnames,
                     input = other_traits_list[[i]]$input)
}




cont_t <- summarise_victories(RR)
```

\

```{r,dpi=dpi, fig.height=contest_h, fig.width=9, fig.cap=paste0("2.4.",i, ". Head to head performance for '",tolower(other_traits_list[[i]]$fullname),"'.")}
plot(cont_t) + 
  labs(y = "", x = "") + 
  theme_minimal() +  
  theme(axis.text.x = element_text(size = 10, color = "#000000"),
        axis.text.y = element_text(size = 10, color = "#000000"),
        strip.text.x = element_text(size = 11, color = "#000000", face = "bold"),
        panel.grid.major = element_blank(),
        panel.grid.minor = element_blank())

```