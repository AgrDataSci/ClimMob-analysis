**`r other_traits_list[[i]]$fullname`**


```{r}
aov_ii <- aov_tables[[i]]
if (length(aov_ii) > 1) {
  kable(aov_ii,
        row.names = FALSE,
        caption = paste0("Table 4.",i,".1. Likelihood ratio test results from fitted Plackett-Luce model with rankings from '", tolower(other_traits_list[[i]]$fullname), "'."))
}

```

\
\

```{r dpi=dpi, fig.height=favplot_h, fig.width=5, fig.cap=paste0("Figure 4.",i,".1. Model coefficients and mean separation of Plackett-Luce model for '", tolower(other_traits_list[[i]]$fullname),"' with ", ci_level*100, "% confidence intervals.")}

if(length(aov_ii) > 1){
  plot(plot_summaries[[i]])
}
```

\

```{r}
if (length(aov_ii) > 1) {
  ms_i <- summaries[[i]]
  kable(ms_i, 
        digits = 4,
        row.names = FALSE,
        caption = paste0("Table 4.",i,".2. Model coefficients and mean separation of Plackett-Luce model for '",
                         tolower(other_traits_list[[i]]$fullname),"' with ", ci_level*100, "% confidence intervals."))
}
```

\

```{r}
if (length(aov_ii) > 1) {
  
  kable(worths[[i]][,-2],
      row.names = FALSE,
      caption = paste0("Table 4.",i,".3 ","Percetage probability of being highest ranked for '",
                      tolower(other_traits_list[[i]]$fullname), "'."))
  
}
```

\

```{r, dpi=dpi, fig.height=favplot_h, fig.width=5, fig.cap=paste0("Figure 4.",i,".2. Probability of being the best ranked for '", tolower(other_traits_list[[i]]$fullname),"'.")}
x <- worths[[i]]
plot_worth_bar(x, value = "Worth", group = Option)
```

